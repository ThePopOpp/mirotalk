# Environment for MiroTalk-SFU
# Production mode
ENVIRONMENT=production

# Public host or domain users will connect to (important for ICE/SDP)
SFU_ANNOUNCED_IP=video.cueallus.com

# App HTTP port inside the container
PORT=3010

# WebRTC media ports (UDP recommended). Widen this for larger rooms.
SFU_MIN_PORT=40000
SFU_MAX_PORT=40100

# Worker processes (start with 1, raise for bigger servers/load)
SFU_NUM_WORKERS=1
